# Food documentation

A personal food log with an Instagram-style feed. Photos from `documenting_food/` are shown by date (filename format: `month:day.order`, e.g. `1:27.1` = January 27, first meal).

Open `index.html` in a browser to view.
